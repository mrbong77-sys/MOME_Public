# Verification and figures.
matplotlib>=3.7

# Refitting the predictor and choosing thresholds.
numpy>=1.24
scikit-learn>=1.3

# Symbolic answer comparison, used by the grader and the fingerprint.
# Pin it: the equivalence verdicts in the published results were produced
# with this version.
math-verify==0.9.0

# The deployed path -- the rule engine, the fingerprint and the score --
# needs none of the above. It is standard library only.
