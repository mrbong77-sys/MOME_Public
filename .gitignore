__pycache__/
*.py[cod]
.venv/
venv/

# Generation records: too large for git, published with the archived dataset.
data/ungated/
data/probes/
data/fingerprints/
data/retry/
data/benchmarks/
data/items/

# Probe text is a derivative of the benchmark items and follows the same
# redistribution policy as they do: the manifests pin it by hash, the text
# itself is regenerated locally.
data/manifests/*_probes.jsonl

# Regenerated outputs.
results/reports/
